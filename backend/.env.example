# Database (default: SQLite in backend directory)
# DATABASE_URL=sqlite:///./msp_leads.db

# JWT secret (CHANGE THIS in production)
JWT_SECRET=change-me-in-production-use-a-real-secret

# API Keys (at least one search key required for real scraping)
SERPER_KEY=
SERPAPI_KEY=
HUNTER_KEY=
APOLLO_KEY=

# CORS origins (comma-separated)
# CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
